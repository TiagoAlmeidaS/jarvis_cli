load("//:defs.bzl", "jarvis_rust_crate")

jarvis_rust_crate(
    name = "app-server",
    crate_name = "codex_app_server",
    integration_deps_extra = ["//jarvis-rs/app-server/tests/common:common"],
    test_tags = ["no-sandbox"],
)
